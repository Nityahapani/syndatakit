# Fidelity Validation

Runs moment-matching tests against published government statistics.
N=50,000 synthetic rows per profile. Results in results/.

## Run it yourself
pip install pandas numpy scipy
python fidelity_engine.py

## Methodology
...cite sources...
